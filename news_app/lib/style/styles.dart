import 'package:flutter/cupertino.dart';

class AppStyles {
  static SizedBox verticallySizedBox = const SizedBox(
    height: 12,
  );
  static BorderRadiusGeometry imageCircularRadius = BorderRadius.circular(12.0);
  static BorderRadiusGeometry boxCircularRadius = BorderRadius.circular(30.0);
  static EdgeInsetsGeometry padding =const EdgeInsets.all(8.0);
}
